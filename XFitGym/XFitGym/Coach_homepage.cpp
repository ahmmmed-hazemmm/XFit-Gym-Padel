#include "Coach_homepage.h"

Coach_homepage::Coach_homepage(QWidget* parent)
    : QWidget(parent)
{
    ui.setupUi(this);
}
Coach_homepage::~Coach_homepage()
{}


Coach_classes::Coach_classes(QWidget* parent)
    : QWidget(parent)
{
    ui.setupUi(this);
}
Coach_classes::~Coach_classes()
{}

